export enum TextInputHierarchy {
  Primary = 'primary',
  Secondary = 'secondary',
  IconIntegrated = 'iconIntegrated',
  Disabled = 'disabled',
  Validated = 'validated',
}
export enum IconPosition {
  Left = 'left',
  Right = 'right',
}
